# Calculator methods:
#   Add, Subtract, Multiply, Divide
def calc_add(a, b):
    if type(a) is str | if type(b) is str:
        return TypeError
    else:
        return a + b

def calc_sub(a, b):
    if type(a) is str | if type(b) is str:
        return TypeError
    else:
        return a - b

def calc_mul(a, b):
    if type(a) is str | if type(b) is str:
        return TypeError
    else:
        return a * b

def calc_div(a, b):
    if type(a) is str | if type(b) is str:
        return TypeError
    else:
        return a / b